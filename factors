#!/usr/bin/python3
import sys

arg = sys.argv
f = open(arg[1], 'r')
for line in f:
    number = int(line)
    for i in range(2, number):
        if (number % i == 0):
            print("{0}={1}*{2}".format(number, number//i, i))
            break
f.close()
