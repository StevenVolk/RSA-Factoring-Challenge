#!/usr/bin/python3
import sys

arg = sys.argv
f = open(arg[-1], 'r')
for line in f:
    number = int(line)
    divisor = set()
    for i in range(2, (number//2)+1):
        if (number % i == 0):
                print("{}={}*{}".format(number, number//i, i))
                break
f.close()
